{{- if .Values.tls.certManager.enabled }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "pureftpd.fullname" . }}-tls
  labels:
    {{- include "pureftpd.labels" . | nindent 4 }}
spec:
  secretName: {{ include "pureftpd.fullname" . }}-tls
  issuerRef:
    name: {{ required "tls.certManager.issuerRef.name is required when certManager.enabled is true" .Values.tls.certManager.issuerRef.name }}
    kind: {{ .Values.tls.certManager.issuerRef.kind }}
  commonName: {{ required "tls.certManager.commonName is required when certManager.enabled is true" .Values.hostname }}
  {{- if .Values.hostname }}
  dnsNames:
    - {{ .Values.hostname }}
  {{- end }}
  duration: {{ .Values.tls.certManager.duration }}
  renewBefore: {{ .Values.tls.certManager.renewBefore }}
{{- end }}
