apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vsftpd.fullname" . }}-users
  labels:
    {{- include "vsftpd.labels" . | nindent 4 }}
data:
  users.conf: |
    {{- range .Values.users }}
    {{ .username }}:{{ .uid }}
    {{- end }}
